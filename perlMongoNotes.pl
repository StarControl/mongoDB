#sub test_url {
  
   # foreach my $key (keys %$doc ) {
   #      my $val=$doc->{$key};
   #           print "$key->$val\n";
   #             }
   #               print "\n";
   #               }
 

 #my $cursor=$urls_coll->find->skip(5);
 #while(my $doc=$cursor->next) {
 # foreach my $key (keys %$doc ){
 #   if($key eq "url"){
 #     print "$doc->{$key}\n";
 #   }
 # }
 #}
